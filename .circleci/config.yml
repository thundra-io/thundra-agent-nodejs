version: 2
jobs:
    build:
      docker:
        - image: circleci/node:8.11
      steps:
        - checkout
        - run: npm install
        - run: npm run lint
        - run: docker-compose up -d
        - run: npm run test-ci
        - run: docker-compose down